In order to install composer you should check current hash in: https://composer.github.io/pubkeys.html and replace the one in the bootstrap.sh script



In order to fast create new virtual host
1. SSH in the machine
2. execute virtualhost.sh in /vagrant

ex: sudo virtualhost create test.local test

